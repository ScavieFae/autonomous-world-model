[package]
name = "hidden-state"
version = "0.1.0"
description = "Hidden state component — Mamba2 SSM recurrent state"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[features]
no-entrypoint = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.29.0"
