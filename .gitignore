.DS_Store
node_modules/
__pycache__/
*.pyc
.env
.venv/
target/
*.so
*.o
dist/

# Animation data (88MB, copied from nojohns/SlippiLab)
viz/zips/

# Quantization output (generated, can be large)
quantization/output/

# Solana build artifacts
solana/.anchor/
solana/test-ledger/

# Weight files (multi-MB, uploaded to chain)
*.bin

# Model checkpoints (downloaded from Modal, not committed)
checkpoints/

# Research paper PDFs (downloaded, not committed)
research/sources/*.pdf

# Simple Loop logs
.loop/logs/

# Bot credentials (never commit)
bot/.env

# Local agent state (not shared)
.claude/last_pull.json
.claude/settings.local.json
.loop/modules/docs/state/site/
